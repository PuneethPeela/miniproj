# ── API Configuration ────────────────────────────────────────────────────────
# Backend API base URL (include /api suffix)
VITE_API_URL=http://localhost:3000/api

# WebSocket server URL (same host, no /api suffix)
VITE_SOCKET_URL=http://localhost:3000
